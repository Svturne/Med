import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RestPassword = () => {
  return (
    <View>
      <Text>RestPassword</Text>
    </View>
  );
};

export default RestPassword;

const styles = StyleSheet.create({});
